import * as React from "react";

export default class EnhancedObjectLiterals extends React.Component {

  constructor(props) {
    super(props);
  }

  render() {
    const x = 1, y = 2, z = 3;

    // before ES6
    // TODO: 將 x、y、z 封裝於 obj1

    // after ES6
    // TODO

    return obj2.x + obj2.y + obj2.z;
  }
}